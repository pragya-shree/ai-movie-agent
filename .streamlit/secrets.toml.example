# Copy this file's contents into your Streamlit Community Cloud app's
# "Secrets" settings panel (Settings → Secrets), or into a local
# .streamlit/secrets.toml for local testing. Never commit the real file.

TMDB_API_KEY = "your_tmdb_api_key_here"

# Used by gemini_client.py — see .env.example for details.
GEMINI_API_KEY = "your_gemini_api_key_here"
